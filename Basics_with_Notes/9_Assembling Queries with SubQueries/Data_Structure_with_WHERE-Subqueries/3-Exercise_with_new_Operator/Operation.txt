-- here we need all values of rows of a column. but (>) returns only single value of a row of a column, so we're going to use (> ALL) operator to 
-- get all values of rows of a column


SELECT name,department, price FROM products
WHERE price > ALL (
	SELECT price FROM products WHERE department = 'Industrial'
);