-- here if we insert a NULL url in photos and then use LEFT JOIN keywords, then left table's NULL row will print, after matches ids

SELECT url,username FROM photos
LEFT JOIN users ON users.id = photos.user_id

-- here left is photos(FROM) and right is users(JOIN)